// Foundation-only placeholder - proves a route resolves to a component and
// that theme colours (text-ink) reach it, nothing more. Replaced page by
// page as real UI gets built.
export function RoutePlaceholder({ name }: { name: string }) {
  return <div className="p-8 font-sans text-ink">{name}</div>;
}
